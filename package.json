{
  "name": "gift-reveal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "prebuild": "node clean-build.js",
    "build": "vite build",
    "postbuild": "node stage-build.js",
    "start": "vite preview",
    "lint": "node --check script.js && node --check worker.js && node --check vite.config.js && node --check clean-build.js && node --check stage-build.js"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.37.1",
    "@openai/sites-vite-plugin": "^0.1.0 || ^0.2.0",
    "vite": "8.0.13"
  }
}
